# .env.example
# Copy this file to .env and fill in your TURN server credentials.
# These are needed when peers cannot connect directly (behind strict NAT/firewall).
#
# You can get temporary TURN credentials from services like:
# - https://dashboard.xirsys.com/ (example)
# or from your own coturn server.

TURN_USERNAME=openrelayproject
TURN_PASSWORD=openrelayproject
PORT=3000

# Optional: port override for server (default 3000)
# PORT=3000
